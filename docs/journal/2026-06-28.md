# 2026-06-28 — planning session (pre-execution)

First working session. Goal: scope ton-2 (Glemton-2) thoroughly enough to run
mostly hands-off, before writing any training code.

## Done
- Post-mortem of Glemton-1: its no-SFT + movie-subtitle design is exactly why it
  doesn't chat. Reusable: `model.py`, `data.py` (uint16 mmap shards), `train.py`,
  `pack_shards.py`, `weekend_run.ps1`. Empirical: 365M hit 12,484 tok/s here
  (but micro=1, only 4.7/12 GB used → big headroom).
- Research: nanochat (full from-scratch ChatGPT pipeline, the template),
  SmolLM2 (data recipe), Phi/Cosmopedia (synthetic-textbook lever), TinyStories
  (small-scale coherence), Chinchilla + inference-optimal (token budget),
  InstructGPT/DPO (post-training). Sources in `PLAN.md` §19.
- Wrote: `PLAN.md`, `docs/character_spec.md`, `docs/hardware_safety.md`,
  `RUNBOOK.md`, repo set up + pushed (github.com/ninjahawk/glemton-2).

## Key decisions
- Base ~180M, vocab 24k, ctx 2048, plain causal SDPA (no FlashAttn/compile —
  flaky on Blackwell/Win, not needed). Inverts Glemton-1: full pretrain → SFT →
  DPO → tool-use pipeline.
- **Twist = trained-in, measurable personality**: witty & deadpan, grounded in
  honesty (`character_spec.md`). Rule: answer first, wit second.
- Teacher for synthetic/SFT data = local Qwen3.6-27B + open datasets, never an
  API. (User's "optics" objection accepted; rationale is data-craft ownership.)
- Night-1 corpus: non-gated only (FineWeb-EDU, Cosmopedia v2, TinyStories,
  OpenWebMath); code deferred; full dedup/decontam = follow-up pass.

## Override exercised (authority granted by ninjahawk today)
User chose "blind auto-chain into the multi-day base pretrain." Overrode to
**gated auto-chain**: the base run earns its multi-day continuation only by
passing G5 (300M-token sanity: loss + samples) and G6 (first-hour thermal
stability). Reason: never commit days of compute to an unvalidated, never-before-
run pipeline; and the first sustained-load thermal behavior of this card must be
observed before trusting multi-day operation. Safety > correctness > progress >
speed.

## Ground truth at session end
No venv, no data (storage cleanup removed both). Python 3.11 present. HF reachable.
GPU idle 46 °C, already power-capped at 175 W (its minimum). 242 GB free.

## Next
Planning ≈ complete. Remaining before execution: ninjahawk's physical checks
(connector seated, airflow) + `/loop` handoff. Then run the gate ladder
G0→G7 per `RUNBOOK.md`, hands-off, with the in-process thermal watchdog binding.
